version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: genius_local
      POSTGRES_USER: genius_user
      POSTGRES_PASSWORD: genius_pass
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: always

volumes:
  pgdata:
